const express=require("express"),multer=require("multer"),imageController=require("../controllers/imageController"),path=require("path"),router=express.Router(),storage=multer.diskStorage({destination:"./dist/tmp/uploads/",filename:(e,r,o)=>{o(null,`${r.fieldname}_${Date.now()}${path.extname(r.originalname)}`)}}),upload=multer({storage:storage});router.post("/upload",upload.single("imagem"),imageController.uploadImage),module.exports=router;